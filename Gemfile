# frozen_string_literal: true

source 'https://rubygems.org'

gem 'rake'
gem 'rubocop'
gem 'simplecov', require: false, group: :test
gem 'flay' # Gem that detects code clones. Use with: flay path/to/folder/*.rb or flay path/to/file.rb
gem 'terminal-table'
